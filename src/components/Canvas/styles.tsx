import styled from "styled-components";

export const CanvasWrapper = styled.div``;

export const CustomCanvas = styled.canvas`
  display: block; /* removes tiny baseline gaps */
  width: 100%;
  height: 100vh;
`;
